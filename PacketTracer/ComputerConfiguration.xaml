<Page
    x:Class="PacketTracer.ComputerConfiguration"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PacketTracer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" SizeChanged="Page_SizeChanged">

    <Grid Name="baseGrid">
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="9*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button Name="consoleButton" Width="200" Height="75" Margin="50,0,0,0" Grid.Column="0" Click="ConsoleButton_Click">Console</Button>
        <Button Name="connectionsButton" Width="200" Height="75" Margin="350,0,0,0" Grid.Column="0" Click="ConnectionsButton_Click">Connections</Button>
        <Button Name="settignsButton" Width="200" Height="75" Margin="650,0,0,0" Grid.Column="0" Click="SettignsButton_Click">Settings</Button>
        <Frame x:Name="frame" Background="Gray" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Height="400" Width="600" />
    </Grid>
</Page>
