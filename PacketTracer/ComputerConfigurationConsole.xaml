<Page
    x:Class="PacketTracer.ComputerConfigurationConsole"
    Height="400"
    Width="600"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PacketTracer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="87*"></RowDefinition>
            <RowDefinition Height="13*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Name="ConsoleTextBlock" x:FieldModifier="public" Grid.Row="0"></TextBlock>
        <TextBox Name="ConsoleInputText" Text="TextBox" Grid.Row="1" TextWrapping="Wrap" Margin="10,10,10,10" KeyDown="ConsoleInputText_KeyDown"/>

    </Grid>
</Page>
